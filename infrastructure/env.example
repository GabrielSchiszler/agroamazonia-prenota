# Copie para infrastructure/.env (gitignored) ou exporte antes de `cdk deploy`.
# Variáveis usadas pelo FrontendStack e pelo synth.
# Modo seguro (obrigatório quando FRONTEND_OPEN_ACCESS não é true)
# OAC_ID=...
# WAF_ARN=arn:aws:wafv2:us-east-1:...
